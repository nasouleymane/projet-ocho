# Copier en .env et renseigner avec les valeurs du dashboard Supabase
# (Project Settings → API). La clé "publishable"/"anon" est faite pour être
# publique côté client — ne jamais mettre la clé "service_role" ici.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
